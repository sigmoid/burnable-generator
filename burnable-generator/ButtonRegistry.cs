using Peridot;

public class ButtonRegistry
{ 
    public static void RegisterButtons()
    {
        // Input buttons are now handled directly with MonoGame's Keyboard.GetState() and Mouse.GetState()
        // No registration needed anymore
    }
}